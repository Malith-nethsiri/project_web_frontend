[phases.setup]
nixPkgs = ["nodejs_18", "npm"]

[phases.install]
cmd = "npm ci"

[phases.build]
cmd = "npm run build"

[phases.start] 
cmd = "npm run start:railway"

[variables]
NODE_ENV = "production"
NEXT_TELEMETRY_DISABLED = "1"